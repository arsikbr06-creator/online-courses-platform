# 🚀 Быстрый старт

## Запуск проекта

### 1. Запустить платформу

```powershell
# Перейти в папку проекта
cd "C:\Users\Admin\Documents\vajno\Cursovaya"

# Запустить все сервисы
docker-compose -f docker-compose.simple.yml up -d

# Проверить статус
docker-compose -f docker-compose.simple.yml ps
```

### 2. Открыть в браузере

- 🌐 **Главная страница:** http://localhost
- 📚 **Каталог курсов:** http://localhost/courses
- 👤 **Регистрация:** http://localhost/register
- 🔑 **Вход:** http://localhost/login

### 3. Остановить проект

```powershell
docker-compose -f docker-compose.simple.yml down
```

---

## 💾 Работа с базой данных

### Подключение к PostgreSQL

```powershell
# Войти в PostgreSQL консоль
docker exec -it cursovaya-postgres-1 psql -U postgres -d online_courses
```

### Полезные SQL команды

```sql
-- Посмотреть все таблицы
\dt

-- Посмотреть курсы
SELECT id, title, image_url, level, duration FROM courses;

-- Посмотреть пользователей
SELECT email, first_name, last_name, created_at FROM users;

-- Посмотреть записи на курсы
SELECT u.email, c.title, uc.enrolled_at, uc.progress 
FROM user_courses uc
JOIN users u ON uc.user_id = u.id
JOIN courses c ON uc.course_id = c.id;

-- Выйти
\q
```

---

## 🎯 Основные функции

### Регистрация и вход

1. Перейдите на http://localhost/register
2. Заполните форму (имя, фамилия, email, пароль)
3. После регистрации автоматически войдёте в систему
4. Для выхода нажмите "Выход" в шапке сайта

### Запись на курсы

1. Войдите в систему
2. Перейдите в **"Курсы"**
3. Нажмите **"Записаться"** на понравившемся курсе
4. Кнопка станет серой с текстом **"Записаны"**
5. Курс появится в **"Личном кабинете"**

### Личный кабинет

- Просмотр всех записанных курсов
- Отслеживание прогресса по каждому курсу
- Статистика обучения

---

## 🛠️ Технологический стек

- **Frontend:** React 18 + Material-UI
- **Backend:** Node.js 20 + Express.js
- **База данных:** PostgreSQL 16
- **Кэш:** Redis 7
- **Веб-сервер:** NGINX
- **Контейнеризация:** Docker + Docker Compose

---

## 📊 Структура базы данных

### Таблицы:
- `users` - пользователи
- `courses` - курсы (с image_url)
- `user_courses` - записи пользователей на курсы
- `modules` - модули курсов
- `lessons` - уроки
- `user_lesson_progress` - прогресс по урокам

---

## ⚠️ Важные замечания

1. **Порт 80** должен быть свободен (сайт работает на http://localhost)
2. **Docker Desktop** должен быть запущен на Windows
3. Для работы БД через pgAdmin порт **15432** (пока не работает, используйте командную строку)
4. При выходе из аккаунта автоматически перенаправляет на главную страницу

---

## 📝 Демо-данные

В базе уже есть 6 курсов:
1. Основы программирования на Python
2. Web-разработка с React
3. Docker и контейнеризация
4. Kubernetes для начинающих
5. DevOps практики
6. Базы данных PostgreSQL

Все курсы с красивыми картинками! 🎨
